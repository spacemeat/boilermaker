------------------ Building $headerDir/$mainHeaderFile.
$<out $headerDir + '/' + $mainHeaderFile>#pragma once

$mainHeaderTopComment

$<if len($enums) $> 0>#include "$headerDir/$enumHeaderFile"
$<endif>$+
$<join for type in $types>#include "$headerDir/$typeHeaderFile"$<delim>
$<endjoin>
$<endout>

