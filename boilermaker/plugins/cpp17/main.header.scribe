$<out $mainHeaderPath>$+
#pragma once

$mainHeaderTopComment

$+  $<if len($enums) $> 0>#include "$enumsHeaderFile"$<endif>
$+  $<if $computeTypes>$<join for type in $bomaTypes>#include "$typeHeaderFile"$<delim>
$<endjoin>$<endif>
$<endout>